/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/the-cutting-bar',
  assetPrefix: '/the-cutting-bar/',
}

export default nextConfig
